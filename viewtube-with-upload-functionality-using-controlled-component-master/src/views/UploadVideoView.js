import React from 'react';

export default function Studio(props) {

  // Declare the state for each of the form field
 
  /* Declare the userError state for defining validation error messages
     Error mesaage for incorrect videourl: `The Url doesn't matches the ViewTube Embed Url Format.`
     Error message for incorrect videoThumbnailUrl: `The Url doesn't matches the ViewTube Embed Url Format.`
     Error message for empty videoTitle:`Video title is required.`
     Error message for empty channelTitle:`Video channel title is required.`
  */

  // Define the handler methods for state change of each of the form fields

  // Define the custom validations for the form fields
  
  // Define the function to handle form submission

  // Define the function to enable/disable submit button based on validations
  
  // Define the reset operation for clearing form data
 
  

  return (
    <Grid container direction="row" justifyContent="flex-start" alignItems="flex-start" style={{ padding: '20px' }}>
     {/* Return the UploadVideo component with required props */}
     <GridItem>
      
     </GridItem>
    </Grid>
  )
}
